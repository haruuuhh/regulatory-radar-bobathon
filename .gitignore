# Maintainer-only generator (keeps the student repo clean). Not needed by students.
build_dataset.py

# Python
__pycache__/
*.pyc
.venv/
venv/
.DS_Store

# Pipeline
pipeline/node_modules/
pipeline/.env
pipeline/.DS_Store
.DS_Store
